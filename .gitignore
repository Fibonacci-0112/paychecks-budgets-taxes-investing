node_modules/
dist/
.turbo/
coverage/
*.tsbuildinfo

.env
.env.local
.env.*.local

.DS_Store
*.log

.expo/
.next/
target/
supabase/.branches/
supabase/.temp/

# Installed agent skills keep their own local trim decision, which is a
# per-checkout choice rather than shared configuration.
.agents/skills/*/.trim-state.json
